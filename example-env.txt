# Copy to `.env` — every value below is optional and falls back to the default shown.
# These match the Compose Postgres service (docker compose up db).

# Database Configuration
DATABASE_HOST=localhost          # default: localhost
DATABASE_PORT=5432               # default: 5432
DATABASE_USERNAME=vapor_username # default: vapor_username
DATABASE_PASSWORD=vapor_password # default: vapor_password

# Database name. When unset, it is chosen by Vapor environment:
#   --env development -> dev   |   --env testing -> test   |   --env production -> vapor_database
DATABASE_NAME=dev

# Logging: trace | debug | info | notice | warning | error | critical
LOG_LEVEL=info
