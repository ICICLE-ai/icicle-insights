Packages
.build
xcuserdata
*.xcodeproj
DerivedData/
.DS_Store
db.sqlite
.swiftpm
.env
.env.*
!.env.example
!.env.container
default.profraw

.memsearch
data/

# Angular frontend. Built output goes to Public/ and is never committed — see the negations
# below, which keep only the legacy Leaf dashboard's assets and disappear along with it.
web/node_modules/
web/dist/
web/.angular/
Public/*
!Public/.gitkeep
!Public/dashboard.css
!Public/dashboard.js
!Public/favicon.ico

# Postgres data bind-mounted by the `just db` container recipe.
.container/

# Personal design notes and presentation drafts.
personal-notes/
